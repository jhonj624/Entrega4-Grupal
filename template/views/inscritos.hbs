{{>header}}

<div class="container"></div>
    
        {{{texto}}}
   
    <div class="row justify-content-md-center align-items-center px-4">
      
        {{#if bandera}}
            {{{mostrarInscrito est}}}
        {{/if}}
    </div>
</div>

{{>footer}}